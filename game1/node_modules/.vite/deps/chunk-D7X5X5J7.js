import {
  i
} from "./chunk-P33LPIMM.js";
import {
  W,
  r,
  s
} from "./chunk-EFD5OXKL.js";
import {
  Types,
  defineComponent,
  defineQuery
} from "./chunk-RNTZHPL5.js";
import {
  Matrix4,
  Quaternion,
  Spherical,
  Vector3
} from "./chunk-LEZHLQNT.js";

// node_modules/vibegame/dist/plugins/orbit-camera/components.js
var i2 = defineComponent({
  target: Types.eid,
  inputSource: Types.eid,
  currentYaw: Types.f32,
  currentPitch: Types.f32,
  currentDistance: Types.f32,
  targetYaw: Types.f32,
  targetPitch: Types.f32,
  targetDistance: Types.f32,
  minDistance: Types.f32,
  maxDistance: Types.f32,
  minPitch: Types.f32,
  maxPitch: Types.f32,
  smoothness: Types.f32,
  offsetX: Types.f32,
  offsetY: Types.f32,
  offsetZ: Types.f32,
  sensitivity: Types.f32,
  zoomSensitivity: Types.f32
});

// node_modules/vibegame/dist/plugins/orbit-camera/math.js
var e = Math.PI * 2;
function r2(t) {
  return (t % e + e) % e;
}
function h(t, o) {
  let n = o - t;
  for (; n > Math.PI; ) n -= e;
  for (; n < -Math.PI; ) n += e;
  return n;
}
function i3(t, o) {
  return 1 - Math.pow(1 - t, o * 60);
}

// node_modules/vibegame/dist/plugins/orbit-camera/operations.js
function d(o, t) {
  const e2 = i3(
    i2.smoothness[o],
    t
  ), s2 = h(
    i2.currentYaw[o],
    i2.targetYaw[o]
  );
  i2.currentYaw[o] += s2 * e2, i2.currentYaw[o] = r2(
    i2.currentYaw[o]
  ), i2.currentPitch[o] += (i2.targetPitch[o] - i2.currentPitch[o]) * e2, i2.currentDistance[o] += (i2.targetDistance[o] - i2.currentDistance[o]) * e2;
}
function C(o, t) {
  const e2 = i2.currentDistance[o], s2 = i2.currentYaw[o], p = Math.PI / 2 - i2.currentPitch[o], c = new Spherical(e2, p, s2);
  return new Vector3().setFromSpherical(c).add(t);
}
function F(o, t, e2) {
  r.posX[o] = t.x, r.posY[o] = t.y, r.posZ[o] = t.z;
  const s2 = new Matrix4();
  s2.lookAt(t, e2, new Vector3(0, 1, 0));
  const p = new Quaternion().setFromRotationMatrix(
    s2
  );
  r.rotX[o] = p.x, r.rotY[o] = p.y, r.rotZ[o] = p.z, r.rotW[o] = p.w, W(r, o);
}

// node_modules/vibegame/dist/plugins/orbit-camera/systems.js
var g = defineQuery([i2, r]);
var M = defineQuery([i2]);
var b = defineQuery([i]);
var Z = {
  group: "setup",
  update: (e2) => {
    const a = g(e2.world);
    for (const t of a) {
      if (i2.target[t] === 0) {
        const n = e2.createEntity();
        e2.addComponent(n, r, {
          scaleX: 1,
          scaleY: 1,
          scaleZ: 1
        }), i2.target[t] = n;
      }
      if (i2.inputSource[t] === 0) {
        const n = b(e2.world);
        if (n.length > 0)
          i2.inputSource[t] = n[0];
        else {
          const i4 = e2.createEntity();
          e2.addComponent(i4, i), i2.inputSource[t] = i4;
        }
      }
    }
  }
};
var R = {
  group: "simulation",
  update: (e2) => {
    const a = M(e2.world);
    for (const t of a) {
      let n = i2.inputSource[t];
      if (!n && e2.hasComponent(t, i) && (n = t, i2.inputSource[t] = t), !n || !e2.hasComponent(n, i))
        continue;
      const i4 = i2.sensitivity[t], s2 = i2.zoomSensitivity[t], y = i.lookX[n], S = i.lookY[n], h2 = i.scrollDelta[n];
      if (i.rightMouse[n] === 1) {
        i2.targetYaw[t] -= y * i4;
        const u = i2.targetPitch[t] + S * i4, p = i2.minPitch[t], f = i2.maxPitch[t];
        i2.targetPitch[t] = Math.max(
          p,
          Math.min(f, u)
        );
      }
      if (h2 !== 0) {
        const m = i2.targetDistance[t], u = i2.minDistance[t], p = i2.maxDistance[t], f = Math.max(0.3, m * 0.08), C2 = h2 * s2 * f, P = m + C2;
        i2.targetDistance[t] = Math.max(
          u,
          Math.min(p, P)
        );
      }
    }
  }
};
var V = {
  group: "draw",
  update: (e2) => {
    const a = g(e2.world);
    for (const t of a) {
      const n = i2.target[t];
      if (!n || !e2.hasComponent(n, s))
        continue;
      d(t, e2.time.deltaTime);
      const i4 = new Vector3(
        s.posX[n] + i2.offsetX[t],
        s.posY[n] + i2.offsetY[t],
        s.posZ[n] + i2.offsetZ[t]
      ), s2 = C(
        t,
        i4
      );
      F(t, s2, i4);
    }
  }
};

export {
  i2 as i,
  Z,
  R,
  V
};
//# sourceMappingURL=chunk-D7X5X5J7.js.map
