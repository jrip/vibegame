import { Font as d } from "@fredli74/typr";
import { readFile as l } from "fs/promises";
import "../plugins/text/components.js";
import "../plugins/text/systems.js";
import { setMeasureFn as h } from "../plugins/text/utils.js";
async function v(t) {
  const e = await l(t);
  return new d(e.buffer);
}
function f(t, e, o) {
  if (!e || !t.hmtx || !t.head) return 0;
  const r = t.stringToGlyphs(e);
  let i = 0;
  for (let n = 0; n < r.length; n++) {
    const s = r[n], c = t.hmtx.aWidth[s] ?? 0;
    if (i += c, n < r.length - 1) {
      const u = r[n + 1], m = t.getPairAdjustment(s, u);
      i += m;
    }
  }
  const a = o / t.head.unitsPerEm;
  return i * a;
}
function p(t) {
  return (e, o) => f(t, e, o);
}
function w(t, e) {
  h(t, p(e));
}
export {
  p as createMeasureFn,
  v as loadFont,
  f as measureTextWidth,
  w as setHeadlessFont
};
//# sourceMappingURL=text.js.map
