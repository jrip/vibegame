import { BuilderOptions } from './builder';
import { State } from './core';
export declare class GameRuntime {
    private state;
    private options;
    private isRunning;
    private isDestroyed;
    private mutationObserver?;
    private canvasElements;
    constructor(state: State, options?: BuilderOptions);
    start(): Promise<void>;
    stop(): void;
    destroy(): void;
    step(deltaTime?: number): void;
    getState(): State;
    private startAnimationLoop;
    private initializeBrowser;
    private processWorldElements;
    private processWorldElement;
    private processWorldContent;
    private validateNoSelfClosingTags;
    private validateXMLStructure;
    private setupMutationObserver;
}
//# sourceMappingURL=runtime.d.ts.map