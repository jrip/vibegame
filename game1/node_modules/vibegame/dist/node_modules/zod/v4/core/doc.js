class h {
  constructor(t = []) {
    this.content = [], this.indent = 0, this && (this.args = t);
  }
  indented(t) {
    this.indent += 1, t(this), this.indent -= 1;
  }
  write(t) {
    if (typeof t == "function") {
      t(this, { execution: "sync" }), t(this, { execution: "async" });
      return;
    }
    const e = t.split(`
`).filter((n) => n), i = Math.min(...e.map((n) => n.length - n.trimStart().length)), s = e.map((n) => n.slice(i)).map((n) => " ".repeat(this.indent * 2) + n);
    for (const n of s)
      this.content.push(n);
  }
  compile() {
    const t = Function, o = this?.args, i = [...(this?.content ?? [""]).map((s) => `  ${s}`)];
    return new t(...o, i.join(`
`));
  }
}
export {
  h as Doc
};
//# sourceMappingURL=doc.js.map
