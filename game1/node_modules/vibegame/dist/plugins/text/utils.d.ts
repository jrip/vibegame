import { State } from '../../core';
import { Text as TroikaText } from 'troika-three-text';
import { Align } from './components';
export type MeasureFn = (text: string, fontSize: number) => number;
export interface TextContext {
    textMeshes: Map<number, TroikaText>;
    textContent: Map<number, string>;
    defaultFont: string | null;
    measureFn: MeasureFn | null;
}
export interface TextBounds {
    width: number;
    height: number;
    blockBounds: [number, number, number, number];
    visibleBounds: [number, number, number, number];
}
export declare function getTextContext(state: State): TextContext;
export declare function setMeasureFn(state: State, fn: MeasureFn): void;
export declare function measureWordWidth(state: State, entity: number): number;
export declare function setTextContent(state: State, entity: number, text: string): void;
export declare function getTextContent(state: State, entity: number): string;
export declare function setDefaultFont(state: State, fontUrl: string | null): void;
export declare function measureText(state: State, entity: number, callback: (bounds: TextBounds) => void): void;
export declare function wordPosition(widths: number[], gap: number, align: Align, index: number): number;
//# sourceMappingURL=utils.d.ts.map