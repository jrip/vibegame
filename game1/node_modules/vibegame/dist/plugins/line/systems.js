import * as p from "three";
import { defineQuery as V } from "bitecs";
import "../../core/ecs/components.js";
import "../../core/validation/schemas.js";
import { WorldTransform as d } from "../transforms/components.js";
import "../transforms/systems.js";
import "../rendering/components.js";
import "../rendering/systems.js";
import { getScene as k, getRenderingContext as C } from "../rendering/utils.js";
import { Line as n } from "./components.js";
import { getLineContext as L, getMaterialKey as v, getOrCreateBatch as A, disposeBatch as M } from "./utils.js";
const B = V([n, d]), S = Math.PI / 6;
function z(a, r, s, e, t) {
  const i = new p.Vector3().subVectors(r, a).normalize(), f = e ? a.clone() : r.clone(), o = e ? i : i.clone().negate(), l = new p.Vector3();
  Math.abs(i.y) < 0.9 ? l.crossVectors(i, new p.Vector3(0, 1, 0)).normalize() : l.crossVectors(i, new p.Vector3(1, 0, 0)).normalize();
  const g = t === 0 ? S : -S, w = o.clone().applyAxisAngle(l, g).multiplyScalar(s), c = f.clone().add(w);
  return { tip: f, wingEnd: c };
}
function y(a, r, s, e, t) {
  a.push(s.x, s.y, s.z, e.x, e.y, e.z), r.push(t.r, t.g, t.b, t.r, t.g, t.b);
}
function D(a) {
  const r = [], s = [];
  for (const e of a)
    if (e.visible) {
      if (y(r, s, e.startPos, e.endPos, e.color), e.arrowStart && e.arrowSize > 0)
        for (let t = 0; t < 2; t++) {
          const { tip: i, wingEnd: f } = z(
            e.startPos,
            e.endPos,
            e.arrowSize,
            !0,
            t
          );
          y(r, s, i, f, e.color);
        }
      if (e.arrowEnd && e.arrowSize > 0)
        for (let t = 0; t < 2; t++) {
          const { tip: i, wingEnd: f } = z(
            e.startPos,
            e.endPos,
            e.arrowSize,
            !1,
            t
          );
          y(r, s, i, f, e.color);
        }
    }
  return { positions: r, colors: s };
}
const q = {
  group: "draw",
  update(a) {
    const r = k(a);
    if (!r) return;
    const s = C(a), e = L(a), t = B(a.world);
    if (s?.renderer) {
      e.resolution.set(
        s.renderer.domElement.width,
        s.renderer.domElement.height
      );
      for (const o of e.batches.values())
        o.material.resolution.copy(e.resolution);
    }
    const i = /* @__PURE__ */ new Map();
    for (const o of t) {
      const l = n.thickness[o], g = n.opacity[o], w = v(l, g), c = new p.Vector3(
        d.posX[o],
        d.posY[o],
        d.posZ[o]
      ), m = new p.Vector3(
        n.offsetX[o] * d.scaleX[o],
        n.offsetY[o] * d.scaleY[o],
        n.offsetZ[o] * d.scaleZ[o]
      ), h = new p.Vector3(
        c.x + m.x,
        c.y + m.y,
        c.z + m.z
      ), b = Math.sqrt(
        n.offsetX[o] ** 2 + n.offsetY[o] ** 2 + n.offsetZ[o] ** 2
      ), x = m.length(), E = b > 0 ? x / b : 1, P = {
        entity: o,
        startPos: c,
        endPos: h,
        color: new p.Color(n.color[o]),
        arrowStart: n.arrowStart[o] === 1,
        arrowEnd: n.arrowEnd[o] === 1,
        arrowSize: n.arrowSize[o] * E,
        visible: n.visible[o] === 1
      };
      let u = i.get(w);
      u || (u = [], i.set(w, u)), u.push(P);
    }
    const f = /* @__PURE__ */ new Set();
    for (const [o, l] of i) {
      f.add(o);
      const g = n.thickness[l[0].entity], w = n.opacity[l[0].entity], c = A(e, o, g, w, r), { positions: m, colors: h } = D(l);
      m.length > 0 ? (delete c.geometry._maxInstanceCount, c.geometry.setPositions(m), c.geometry.setColors(h), c.segments.computeLineDistances(), c.segments.visible = !0) : c.segments.visible = !1;
    }
    for (const [o, l] of e.batches)
      f.has(o) || (M(l, r), e.batches.delete(o));
  }
};
export {
  q as LineSystem
};
//# sourceMappingURL=systems.js.map
