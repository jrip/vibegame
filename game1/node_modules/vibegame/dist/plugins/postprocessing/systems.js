import { EffectComposer as A, RenderPass as G, SMAAEffect as L, BloomEffect as z, ToneMappingEffect as P, EffectPass as D } from "../../node_modules/postprocessing/build/index.js";
import * as Q from "three";
import { defineQuery as T } from "bitecs";
import "../../core/ecs/components.js";
import "../../core/validation/schemas.js";
import { WorldTransform as V } from "../transforms/components.js";
import "../transforms/systems.js";
import { MainCamera as v } from "../rendering/components.js";
import "../rendering/systems.js";
import { getRenderingContext as H, getScene as W, threeCameras as j } from "../rendering/utils.js";
import { Bloom as l, Dithering as p, SMAA as C, Tonemapping as s } from "./components.js";
import { DitheringEffect as k } from "./effects/dithering-effect.js";
import { getPostprocessingContext as B } from "./utils.js";
const q = T([v, V]), F = T([v]), ee = {
  group: "draw",
  update(o) {
    const i = H(o), m = B(o), c = W(o);
    if (!i.renderer || !c) return;
    const d = q(o.world);
    for (const e of d) {
      const a = j.get(e);
      if (!a) continue;
      let r = m.composers.get(e);
      r || (r = new A(i.renderer), r.addPass(new G(c, a)), m.composers.set(e, r), m.effects.set(e, /* @__PURE__ */ new Map()));
      const t = m.effects.get(e), g = t.get("bloom"), h = t.get("dithering"), w = t.get("smaa"), u = t.get("tonemapping"), S = o.hasComponent(e, l), b = o.hasComponent(e, p), R = o.hasComponent(e, C), x = o.hasComponent(e, s);
      if (R) {
        if (!w) {
          const n = C.preset[e], y = new L({
            preset: n
          });
          t.set("smaa", y), f(r, t, a);
        }
      } else w && (t.delete("smaa"), f(r, t, a));
      if (S)
        if (g) {
          const n = g;
          n.intensity = l.intensity[e], n.luminanceMaterial.uniforms.threshold.value = l.luminanceThreshold[e];
        } else {
          const n = new z({
            intensity: l.intensity[e],
            luminanceThreshold: l.luminanceThreshold[e],
            mipmapBlur: l.mipmapBlur[e] === 1,
            radius: l.radius[e],
            levels: l.levels[e]
          });
          t.set("bloom", n), f(r, t, a);
        }
      else g && (t.delete("bloom"), f(r, t, a));
      if (b)
        if (h) {
          const n = h;
          n.colorBits = p.colorBits[e], n.intensity = p.intensity[e], n.grayscale = p.grayscale[e] === 1, n.scale = p.scale[e], n.noise = p.noise[e];
        } else {
          const n = new k({
            colorBits: p.colorBits[e],
            intensity: p.intensity[e],
            grayscale: p.grayscale[e] === 1
          });
          t.set("dithering", n), f(r, t, a);
        }
      else h && (t.delete("dithering"), f(r, t, a));
      if (x)
        if (u) {
          const n = u;
          if (n.middleGrey !== s.middleGrey[e] || n.whitePoint !== s.whitePoint[e] || n.averageLuminance !== s.averageLuminance[e]) {
            const y = new P({
              mode: s.mode[e],
              middleGrey: s.middleGrey[e],
              whitePoint: s.whitePoint[e],
              averageLuminance: s.averageLuminance[e],
              adaptationRate: s.adaptationRate[e]
            });
            t.set("tonemapping", y), f(r, t, a);
          } else
            n.mode = s.mode[e];
        } else {
          const n = new P({
            mode: s.mode[e],
            middleGrey: s.middleGrey[e],
            whitePoint: s.whitePoint[e],
            averageLuminance: s.averageLuminance[e],
            adaptationRate: s.adaptationRate[e]
          });
          t.set("tonemapping", n), f(r, t, a);
        }
      else u && (t.delete("tonemapping"), f(r, t, a));
      const E = i.renderer.getSize(new Q.Vector2());
      r.setSize(E.width, E.height);
    }
    for (const [e, a] of m.composers)
      o.exists(e) || (a.dispose(), m.composers.delete(e), m.effects.delete(e));
  }
};
function f(o, i, m) {
  for (; o.passes.length > 1; )
    o.removePass(o.passes[1]);
  if (i.size > 0) {
    const c = [];
    i.has("smaa") && c.push(i.get("smaa")), i.has("bloom") && c.push(i.get("bloom")), i.has("dithering") && c.push(i.get("dithering")), i.has("tonemapping") && c.push(i.get("tonemapping"));
    const d = new D(m, ...c);
    o.addPass(d);
  }
}
const te = {
  group: "draw",
  last: !0,
  update(o) {
    const i = B(o), m = F(o.world);
    if (m.length === 0) return;
    const c = m[0], d = i.composers.get(c);
    d && d.render();
  }
};
export {
  te as PostprocessingRenderSystem,
  ee as PostprocessingSystem
};
//# sourceMappingURL=systems.js.map
