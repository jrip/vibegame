import { defineQuery as C } from "bitecs";
import { NULL_ENTITY as u } from "../../core/ecs/constants.js";
import { Parent as d } from "../../core/ecs/components.js";
import "three";
import "../../core/validation/schemas.js";
import { InterpolatedTransform as L, CharacterController as S, CharacterMovement as E } from "../physics/components.js";
import "../physics/recipes.js";
import "../physics/systems.js";
import "../physics/utils.js";
import { InputState as h } from "../input/components.js";
import "../input/systems.js";
import { AnimatedCharacter as t } from "./components.js";
import { ANIMATION_STATES as n, ANIMATION_CONFIG as g } from "./constants.js";
import { createBodyPart as m, resetBodyPartTransforms as D, applyWalkAnimation as I, applyLandingAnimation as P, applyFallAnimation as b, applyJumpAnimation as Y } from "./utils.js";
const N = C([t]), B = {
  group: "setup",
  update(r) {
    const p = N(r.world).filter(
      (i) => t.headEntity[i] === u
    );
    for (const i of p)
      t.headEntity[i] = m(
        r,
        i,
        "head"
      ), t.torsoEntity[i] = m(
        r,
        i,
        "torso"
      ), t.leftArmEntity[i] = m(
        r,
        i,
        "leftArm"
      ), t.rightArmEntity[i] = m(
        r,
        i,
        "rightArm"
      ), t.leftLegEntity[i] = m(
        r,
        i,
        "leftLeg"
      ), t.rightLegEntity[i] = m(
        r,
        i,
        "rightLeg"
      );
  }
}, Q = {
  group: "simulation",
  update(r) {
    const p = N(r.world), i = r.time.deltaTime, c = r.time.fixedDeltaTime;
    for (const e of p) {
      if (d.entity[e] === u) continue;
      const o = d.entity[e], T = L.posY[o], G = L.prevPosY[o], M = S.grounded[o] === 1;
      let l = !1;
      if (r.hasComponent(o, h)) {
        const y = h.moveX[o], f = h.moveY[o];
        l = Math.abs(y) > 0.1 || Math.abs(f) > 0.1;
      }
      const v = (T - G) / c;
      let A = 1;
      if (r.hasComponent(o, E) && l) {
        const y = E.actualMoveX[o], f = E.actualMoveZ[o];
        A = Math.sqrt(y * y + f * f) / c;
      }
      const s = t.animationState[e];
      let a = s;
      switch (D(
        t.headEntity[e],
        t.torsoEntity[e],
        t.leftArmEntity[e],
        t.rightArmEntity[e],
        t.leftLegEntity[e],
        t.rightLegEntity[e]
      ), M ? s === n.FALLING || s === n.JUMPING ? (a = n.LANDING, t.stateTransition[e] = 0) : t.animationState[e] === n.LANDING ? (t.stateTransition[e] += i, t.stateTransition[e] >= g.landing.duration ? a = l ? n.WALKING : n.IDLE : a = n.LANDING) : l ? (a = n.WALKING, t.phase[e] += i * A * g.frequency, t.phase[e] >= 1 && (t.phase[e] -= 1)) : a = n.IDLE : v > 1 ? (a = n.JUMPING, s !== n.JUMPING && (t.jumpTime[e] = 0), t.jumpTime[e] += i) : (a = n.FALLING, s !== n.FALLING && (t.fallTime[e] = 0), t.fallTime[e] += i), t.animationState[e] = a, a) {
        case n.WALKING:
          I(
            t.leftArmEntity[e],
            t.rightArmEntity[e],
            t.leftLegEntity[e],
            t.rightLegEntity[e],
            t.phase[e]
          );
          break;
        case n.JUMPING:
          Y(
            t.headEntity[e],
            t.torsoEntity[e],
            t.leftArmEntity[e],
            t.rightArmEntity[e],
            t.leftLegEntity[e],
            t.rightLegEntity[e],
            t.jumpTime[e]
          );
          break;
        case n.FALLING:
          b(
            t.headEntity[e],
            t.torsoEntity[e],
            t.leftArmEntity[e],
            t.rightArmEntity[e],
            t.leftLegEntity[e],
            t.rightLegEntity[e],
            t.fallTime[e]
          );
          break;
        case n.LANDING:
          P(
            t.headEntity[e],
            t.torsoEntity[e],
            t.stateTransition[e]
          );
          break;
        case n.IDLE:
        default:
          I(
            t.leftArmEntity[e],
            t.rightArmEntity[e],
            t.leftLegEntity[e],
            t.rightLegEntity[e],
            0
          );
          break;
      }
    }
  }
};
export {
  B as AnimatedCharacterInitializationSystem,
  Q as AnimatedCharacterUpdateSystem
};
//# sourceMappingURL=systems.js.map
