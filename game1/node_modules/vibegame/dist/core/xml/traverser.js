function h(d, o) {
  function r(n) {
    !o.onElement?.(n) && o.onUnhandled && o.onUnhandled(n);
    for (const f of n.children)
      r(f);
  }
  for (const n of d.children)
    r(n);
}
function i(d, o) {
  const r = [];
  function n(c) {
    o(c) && r.push(c);
    for (const f of c.children)
      n(f);
  }
  for (const c of d.children)
    n(c);
  return r;
}
export {
  i as findElements,
  h as traverseElements
};
//# sourceMappingURL=traverser.js.map
