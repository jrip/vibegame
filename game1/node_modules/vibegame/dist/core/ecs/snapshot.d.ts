import { State } from './state';
export interface SnapshotOptions {
    entities?: string[];
    components?: string[];
    includeSequences?: boolean;
}
export interface SequenceSnapshot {
    name: string;
    eid: number;
    state: 'idle' | 'playing';
    currentIndex: number;
    itemCount: number;
    progress: number;
}
export interface EntitySnapshot {
    eid: number;
    name?: string;
    components: Record<string, Record<string, number>>;
}
export interface WorldSnapshot {
    elapsed: number;
    entities: EntitySnapshot[];
    sequences?: SequenceSnapshot[];
}
export declare function createSnapshot(state: State, options?: SnapshotOptions): WorldSnapshot;
export declare function formatSnapshot(snapshot: WorldSnapshot): string;
//# sourceMappingURL=snapshot.d.ts.map