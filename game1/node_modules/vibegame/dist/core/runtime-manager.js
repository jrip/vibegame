const e = [];
function t(i) {
  e.push(i);
}
function o(i) {
  const n = e.indexOf(i);
  n !== -1 && e.splice(n, 1);
}
function s() {
  if (e.length > 0) {
    console.warn(
      `[VibeGame] Disposing ${e.length} active runtime(s)`
    );
    for (const i of e)
      try {
        i.destroy();
      } catch (n) {
        console.error("[VibeGame] Failed to dispose runtime:", n);
      }
    e.length = 0;
  }
}
export {
  s as disposeAllRuntimes,
  t as registerRuntime,
  o as unregisterRuntime
};
//# sourceMappingURL=runtime-manager.js.map
